package com.ppms.shift;

public enum DiscrepancyType {
    SHORT,  // operator collected less than what meter reading indicates
    OVER    // operator collected more than what meter reading indicates
}
